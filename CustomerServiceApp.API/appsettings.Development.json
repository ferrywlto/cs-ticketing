{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "PasswordHasher": {
    "_comment": "Salt and Iterations are configured via User Secrets in development. Run: dotnet user-secrets set 'PasswordHasher:Salt' 'your-secure-salt-here'"
  },
  "JwtToken": {
    "_comment": "JWT configuration is set via User Secrets in development. Run: dotnet user-secrets set 'JwtToken:SecretKey' 'your-secure-jwt-secret-key-here'"
  }
}
